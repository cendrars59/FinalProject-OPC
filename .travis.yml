dist: bionic
language: python
python:
  - "3.8"

init:
    pip install pipenv
    
addons:
  postgresql: "12"
  apt:
    packages:
    - postgresql-12
    - postgresql-client-12
  ## using chrome and firefox for Selenium 
  chrome: stable
  firefox: latest


branches:
  only:
      - feature/continous
      - develop
      

services:
    - postgresql

install:
  pipenv install
  


env: DJANGO_SETTINGS_MODULE="config.settings"
global:
  - PGPORT=5433
  - PGUSER=travis
  - SECRET_KEY='elvisisNotDe@d' 
  - DATABASE='final'
  - USERAPP='travis'
  - PASSWORD='passw@rdMF'
  - DEBUG='True'

before_script:
  - install-webdrivers --path /usr/local/bin/
  - psql -c 'create database final;' -U postgres

# command to run tests
script:
  - pytest --cov . # or py.test for Python versions 3.5 and below
  - flake8
